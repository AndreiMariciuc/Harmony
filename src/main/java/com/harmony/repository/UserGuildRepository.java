package com.harmony.repository;

import com.harmony.model.UserGuild;
import org.springframework.data.repository.CrudRepository;

public interface UserGuildRepository extends CrudRepository<UserGuild, Long> {
}
